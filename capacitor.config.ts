import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.doigyfu.kitsune',
  appName: 'kitsune',
  webDir: 'dist/kitsune',
  bundledWebRuntime: false
};

export default config;
